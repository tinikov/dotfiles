[init]
	defaultBranch = main

# Refuse to commit if no identity is set (no hostname-based guessing)
[user]
	useConfigOnly = true

# Machine-specific identity (user.name / user.email), written by init.sh
[include]
	path = ~/.gitconfig.local
